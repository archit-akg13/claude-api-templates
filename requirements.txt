# Core Anthropic SDK — used by every template in this repo.
anthropic>=0.40.0

# Data validation for structured_output.py.
pydantic>=2.6.0

# Token counting helpers used by token_counter.py and prompt_cache_helper.py.
tiktoken>=0.7.0

# Async HTTP for batch_processor.py concurrency.
httpx>=0.27.0

# PDF text extraction for pdf_qa.py and regex_pdf_field_extractor.py.
pypdf>=4.2.0

# Image handling for vision_extractor.py (resize/encode).
Pillow>=10.3.0

# Optional: load API keys from a .env file during local development.
python-dotenv>=1.0.0
